_target_: lib.datamodule.general_datamodule_v2.GeneralDataModule
dataset_opts:
  val:
    _target_: lib.dataset.mde.hypersim.Dataset
    data_root: ${oc.env:workspace}/hypersim
    split: 'val'
    frames: [0, -1, 1]
    depth_norm: 'nonorm'
    resize_ratio: 0.625
loader_opts:
  val:
    batch_size: 1
    num_workers: 12